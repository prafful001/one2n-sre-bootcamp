apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: {{ .Values.namespace }}
data:
  DATABASE_URL: {{ .Values.config.databaseUrl }}
  FLASK_APP: {{ .Values.config.flaskApp }}
  FLASK_ENV: {{ .Values.config.flaskEnv }}
